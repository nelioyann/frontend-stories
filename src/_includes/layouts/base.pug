doctype html
html(lang="en")
    include ../partials/head.pug
    body(data-layout="cover")
        include ../partials/header.pug
        main(data-props="centered")
            b #{title}
            p #{description}
            ol
                for page, i in database
                    li #{page.name}
        include ../partials/footer.pug
