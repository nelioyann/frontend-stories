<li>
    <article>
        <a itemprop="url" data-button="link" href="/{{ entry.slug }}">
            {{ entry.name }}</a>
        {# <p>
                    <time itemprop="dateModified" datetime="{{ story.edited }}">{{entry.edited | readableDate}}</time>
                </p> #}
        <p>{{entry.description}}</p>
    </article>
</li>