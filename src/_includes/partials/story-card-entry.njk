<article>
    <a class="headline-entry" itemprop="url" data-button="link" href="/{{ entry.slug }}" data-icon="{{entry.icon}}">
        {{ entry.name }}
    </a>
    <p>
        {{entry.excerpt}}
    </p>
</article>