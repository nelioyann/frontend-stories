<li itemscope itemtype="http://schema.org/Article" class="story-card" data-props="noMeasure">
    {% set color = entry.category.color %}
    {% set type = entry.type.name %}
    <span class="story-card__cover" data-color="{{ color }}" aria-hidden="true">
        <span data-font="emoji" aria-hidden="true">{{ entry.icon if entry.icon else '📄' }}</span>
    </span>
    <a itemprop="url" data-link="noHover" class="story-card__title" href="/{{ entry.slug }}">{{ entry.name }}</a>
    <meta itemprop="headline" content="{{ entry.name }}"/>
</li>