---
layout: layouts/home.njk
title: Homepage
description: Front stories with references
---

<ul>
    {%- for story in stories -%}
        {% set url = ["/stories/", story.name | slugify] | join %}
        <li>
            <a href="{{ url }}">{{ story.name }}</a>
        </li>
    {%- endfor -%}
</ul>