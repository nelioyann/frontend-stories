---
layout: layouts/nested-page-layout.njk
title: All Stories
description: All stories on the site grouped by categories.
tags: 
    - webpage
permalink: /stories/index.html
eleventyNavigation:
  key: Stories
  parent: Home
---

<h1 data-heading="3">
    {{ title }}
</h1>
<p>
    {{ description }}
</p>
<ul data-layout="stack" data-props="noMeasure space:xl">
    {%- for category in collections.categories %}
        <li>
            <span data-heading="5">{{ category }}</span>
            <ul data-layout="stack">
                {% for story in collections.stories | filterByCategory(category) %}
                    <li data-props="noMeasure">
                        <a data-link class="story-card__title" href="/{{ story.slug }}">
                            <span>
                                {{ story.name }}
                            </span>
                        </a>
                    </li>
                {% endfor %}
            </ul>
        </li>
    {% endfor -%}
</ul>