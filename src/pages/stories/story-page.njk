---
pagination: 
    data: collections.stories
    size: 1
    alias: story
    addAllPagesToCollections: true
layout: layouts/base-layout.njk
permalink: /{{story.slug}}/
og_type: "article"
eleventyComputed:
    title: "{{ story.name | truncate(150) }} "
    thumbnail: "{{meta.site_url}}/images/og-images/og-{{ story.slugs }}.jpeg"
    emojicon: "{{ story.icon }}"
    description: "{{story.excerpt}}"
    eleventyNavigation:
        key: "{{story.id}}"
        title: "{{story.name}}"
        parent: Stories
---

{% extends "layouts/story-layout.njk" %}
{% set story_contribution_url = contribute.tally_url+"?slugs="+story.slugs+"&title="+story.name+"&category="+story.category.name %}
{# {{ contribute.tally_url }}?slugs={{ story.slugs }}&title={{ story.name }}&category={{ story.category.name }}&description={{ story.description }} #}
{{ story_contribution_url |log }}
{% block aside %}
    <p>
        <span data-font="emoji">📅</span>
        <time itemprop="datePublished" datetime="{{ story.created }}">{{ story.created | readableDate }}</time>
        <meta itemprop="dateModified" datetime="{{ story.edited }}" content="{{ story.edited }}"/>
    </p>
    <p>
        <span data-font="emoji">⏱</span>
        {{ story.description | emojiReadTime }}
    </p>
    <p itemprop="author" itemscope itemtype="http://schema.org/Person">
        <span data-font="emoji">👤</span>
        <span itemprop="name">
            {{ meta.author.name }}
        </span>
        <link itemprop="url" href="{{ meta.author.url }}"/>
    </p>
    {# <figure>
                                                                    {{ meta.site_url| qrcode | safe }}
                                                                    <figcaption class="visually-hidden">Share link</figcaption>
                                                                </figure> #}
{% endblock %}
{% block main %}
    <header
        aria-labelledby="story-name" data-layout="stack" id="cover" data-props="space:s">
        {# {% if emojicon %}
                                                                                                            <span aria-hidden data-font="emoji">{{ emojicon }}</span>
                                                                                                        {% endif %} #}
        <h1 itemprop="headline" id="story-name" data-heading="1" data-props="centered noBorder noMeasure">
            <b>{{ story.name }}</b>
        </h1>
        <link itemprop="thumbnailUrl" href="{{ story.thumbnail }}"/> {% set type = story.type.name %}
        {% if type == "Insight" %}
            <p data-layout="box" data-heading="6" data-props="invert noMeasure noBorder">
                <span aria-hidden="true" data-font="emoji">ℹ</span>
                This entry is flagged as
                <code>Insight</code>
                which means that it lacks research data. You can help improve this page by
                <a href="{{ story_contribution_url }}">providing feedback</a>
            </p>
        {% endif %}
        <meta itemprop="description" content="{{ story.excerpt }}"/>
        <meta itemprop="teaches" content="{{ story.category.name }}"/>
    </header>
    <section aria-labelledby="summary">
        <div data-layout="stack" data-props="space:xs">
            <h2 class="visually-hidden" id="summary" data-heading="2 anchor">
                <a href="#summary">Summary</a>
            </h2>
            <div itemprop="articleBody" data-layout="stack" data-markdown>
                {% renderTemplate "md, liquid", story %}
                {{ description }}
                {% endrenderTemplate %}
            </div>
        </div>
    </section>
    <section aria-labelledby="references">
        <div data-layout="stack" data-props="space:xs">
            <h2 id="references" data-heading="anchor">
                <a href="#references">References</a>
            </h2>
            <ul role="list" data-layout="stack" data-props="noPad x:stretch space:xs">
                {%- for reference in story.references %}
                    {% include "partials/reference-item.njk" %}
                {% endfor -%}
            </ul>
        </div>
    </section>
    <section aria-labelledby="contribute">
        <div data-layout="stack" data-props="space:xs">
            <h2 id="contribute" data-heading="anchor">
                <a href="#contribute">Contribute and give feedback</a>
            </h2>
            <p>
                This entry is flagged as Insight, indicating that it lacks research references. Please know that you can help improve
                this page by
                {# <b> #}
                    <a data-link href="{{ story_contribution_url }}" target="_blank">providing feedback</a>
                {# </b> #}
            </p>
        </div>
    </section>
    <section aria-label="Navigate" data-layout="stack">
        <hr data-props="space:0"/>   

        <h2 data-heading="6" id="pagination-nav" data-props="space:s">Navigate to other stories</h2>
        <nav
            class="story-pagination-navigation"
            aria-labelledby="pagination-nav"
            data-layout="switcher"
            data-props="x:between y:stretch">
            {% if pagination.href.previous %}
                <a data-button data-layout="stack" class="previous-button" data-props="space:0" href="{{ pagination.href.previous }}">
                    <span>
                        <span aria-hidden="true" data-font="emoji">◀️</span>
                        Previous item</span>
                    <span>{{ pagination.page.previous.name | truncate(40) }}</span>
                </a>
            {% endif %}
            {% if pagination.href.next %}
                <a data-layout="stack" class="next-button" data-button data-props="space:0 y:end" href="{{ pagination.href.next }}">
                    <div>
                        <span>
                            Next item
                        </span>
                        <span aria-hidden="true" data-font="emoji">▶️</span>
                    </div>
                    <p>{{ pagination.page.next.name | truncate(40) }}</p>
                </a>
            {% endif %}
        </nav>
    </section>
{% endblock %}